/** Centralizes immutable application identity used by adapters. */
export const ApplicationMetadata = {
  name: "atlas",
  description: "Atlas Visual Validation Platform",
  version: "0.1.0"
} as const;
