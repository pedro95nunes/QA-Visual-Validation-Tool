import { AtlasException } from "./atlas.exception";

/** Indicates that an Action could not be resolved or its configuration is invalid. */
export class ActionConfigurationException extends AtlasException {}
